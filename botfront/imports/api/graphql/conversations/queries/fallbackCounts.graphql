query FallbackCounts(
    $projectId: String!,
    $from: Float,
    $to: Float,
    $fallbacks: [String] = ["utter_fallback"],
    $nBuckets: Int = 7,
) {
    responseCounts(
        projectId: $projectId,
        from: $from,
        to: $to,
        responses: $fallbacks,
        nBuckets: $nBuckets,
    ) {
        bucket, count, total, proportion
    }
}