query FallbackCounts(
    $projectId: String!,
    $envs: [String] = ["development", null],
    $langs: [String] = [],
    $from: Float,
    $to: Float,
    $include: [String] = ["action_botfront_fallback"],
    $nBuckets: Int = 7,
) {
    actionCounts(
        projectId: $projectId,
        envs: $envs,
        langs: $langs,
        from: $from,
        to: $to,
        include: $include,
        nBuckets: $nBuckets,
    ) {
        bucket, hits, count, proportion
    }
}