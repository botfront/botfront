query ConversationLengths(
    $projectId: String!,
    $envs: [String] = ["development", null],
    $from: Float,
    $to: Float
) {
    conversationLengths(
        projectId: $projectId,
        envs: $envs,
        from: $from,
        to: $to,
    ) {
        frequency,
        count,
        length
    }
}