type Query {
    intentFrequencies(
        projectId: String!,
        from: Int = 0,
        to: Int,
        position: Int = 1,
        exclude: [String] = [],
        only: [String] = [],
        first: Int,
        last: Int,
        beg: Int,
        end: Int,
    ): [IntentFrequency!]!
}

type IntentFrequency {
    name: String,
    frequency: Float,
    count: Int,
}
