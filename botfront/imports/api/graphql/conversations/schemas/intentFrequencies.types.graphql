type Query {
    intentFrequencies(
        projectId: String!,
        envs: [String],
        from: Float = 0,
        to: Float,
        exclude: [String] = [],
        only: [String] = [],
        first: Int,
        last: Int,
        beg: Int,
        end: Int,
    ): [IntentFrequency!]!
}

type IntentFrequency {
    name: String,
    frequency: Float,
    count: Int,
}
