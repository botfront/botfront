type Query {
    conversationDurations(
        projectId: String!,
        from: Int = 0,
        to: Int,
        cuttoffs: [Int] = [30, 60, 90, 120, 180],
    ): [ConversationDurations!]!
}

type ConversationDurations {
  duration: String,
  frequency: Float,
  count: Int,
}
