type Query {
    conversationDurations(
        projectId: String!,
        from: Int = 0,
        to: Int,
        cuttoffs: [Int] = [30, 60, 90, 120, 180],
    ): [ConversationDuration!]!
}

type ConversationDuration {
  duration: String,
  frequency: Float,
  count: Int,
}
