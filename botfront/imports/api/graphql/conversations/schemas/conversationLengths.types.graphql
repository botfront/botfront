type Query {
    conversationLengths(
        projectId: String!,
        envs: [String],
        from: Float = 0,
        to: Float
    ): [ConversationLength!]!
}

type ConversationLength {
  length: Int,
  frequency: Float,
  count: Int,
}
