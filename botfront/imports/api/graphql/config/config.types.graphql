type Query {
    getConfig(
        projectId: String!,
        output: String,
        environment: String,
    ): Config

    healthCheck: Boolean!
}



type Config {
    credentials: Any,
    endpoints: Any,
}
