scalar Date

scalar Any

type PageInfo {
    endCursor: String!,
    hasNextPage: Boolean!
}

type batchMutationOutput {
    nTotal: Int
    nPushed: Int
    nInserted: Int
    nUpdated: Int
    failed: [String]
}

type updateResponse {
    success: Boolean!
}